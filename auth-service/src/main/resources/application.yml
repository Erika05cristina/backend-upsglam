server:
  port: 8081

spring:
  application:
    name: auth-service

jwt:
  secret: ${JWT_SECRET}

firebase:
  service-account-path: classpath:firebase-key.json
  api-key: ${JWT_SECRET}

user-service:
  base-url: ${USER_SERVICE_URL:http://localhost:8085}
